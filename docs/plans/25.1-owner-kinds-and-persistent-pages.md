# Slice 25.1 — Distinguish owner kinds and introduce persistent pages

**Status:** in progress.

## Goal

Make `kind: 'root' | 'subproject'` a stored, enforced distinction and give every project a
persisted canonical page that owns its sections, in one schema-version cutover that a bounded
converter carries live data across.

## Spec sections

§11–15 (contracts, storage, persistence behaviour, the one-off converter), §26–28 (the two
project kinds, pages, the canvas), §33 (task model, unchanged but re-read), §45 (Clock).

Roadmap: [25-multi-page-projects-overhaul.md](25-multi-page-projects-overhaul.md) §25.1.
Decision:
[2026-09-project-workspaces-and-subproject-work-units.md](../decisions/2026-09-project-workspaces-and-subproject-work-units.md).

## Acceptance check

1. `pnpm test`, `pnpm lint` and `pnpm build` are green. Baseline before this phase: contracts,
   repositories, prototype-data (62), domain (271), mcp-tools (78), prototype-host (152) and
   web all passing.
2. `pnpm prototype:seed nested-projects` writes `.prototype/data.json` with
   `"schemaVersion": 3`, every project carrying a `kind`, and every section a `pageId`. A host
   route test (`apps/prototype-host/api/routes.test.ts`) covers that the document still serves
   `GET /api/projects`, so this does not depend on a running dev host.
3. `pnpm prototype:upgrade <path>` over the committed v2 fixture
   `packages/prototype-data/test/fixtures/nested-projects-v2.json` produces a document
   `validateDocumentIntegrity` accepts, preserving every id, every `archivedAt`,
   `archivedWithSectionId` and every section `position`. Re-running it is a no-op.
4. A converter run whose result fails validation writes **nothing** — no backup, no target,
   no temp file left behind. Asserted through injected file operations (test 27), not by
   inspecting a disk.
5. A failed page insert during project creation leaves neither project nor page. Discharged by
   test 14; there is no way to force it over HTTP, so it is a domain check, not a manual one.

No navigation, no page-selection API, no shortcut operations — 25.2, 25.3 and 25.4.

## Design decisions this plan makes

**A subproject's work canvas is a page record too**, `kind: 'work'`. Page kinds are
`home | work | todos | archive | reflections`. The alternative — sections on a subproject
pointing at no page — makes `pageId` optional and puts a branch in every reader. "The canonical
page of a project" is then one lookup: `home` for a root, `work` for a subproject.

`work` is deliberately **not** the fifth kind the decision entry's *Revisit when* set a
tripwire for. That tripwire is about a growing menu of pages a person can enable; `work` cannot
be added, disabled, chosen or navigated to. The decision entry now says so in its own words,
and §26 lists it separately from the four navigable kinds.

**Root variants declare `parentProjectId: z.undefined().optional()`** rather than omitting the
key, so `project.parentProjectId` keeps compiling across the union at the ~dozen existing read
sites (`data-store.ts:116–120,251`; `project-service.ts:78,124,130,197–198`;
`json-repositories.ts:78`; `seeds.ts:59`; `fake-gateway.ts:78`; `sub-projects-store.ts`;
timeline and workspace services) while a root carrying a parent still fails to parse. Verified
by running it against the pinned zod 4.4.3 and by `tsc`.

The **`null` path needs its own answer.** `UpdateProjectInput.parentProjectId` is
`.nullable()`, and `ProjectService.update`'s `apply` *deletes* the key on `null` — today that
promotes a subproject to a root. Under the union it would instead surface as a raw `ZodError`
out of `commit`. `parentProjectId` therefore **stops being nullable** in
`UpdateProjectInputSchema`, and the service refuses a kind change explicitly with a
`DomainRuleError` before it reaches `ProjectSchema.parse`. Promotion and demotion are
root/subproject conversion, which the roadmap lists as a non-goal.

**`completedAt` lands on both kinds**, stamped when `status` becomes `completed` and cleared
when it leaves. Recorded in the decision entry as well as here, because plans are archived and
the roadmap asked for a justification.

## File-level change list

### Contracts — `packages/contracts/src`

| File | Responsibility |
|---|---|
| `ids.ts` | `ProjectPageIdSchema`/`ProjectPageId`, `SectionShortcutIdSchema`/`SectionShortcutId` |
| `project-page.ts` **new** | `ProjectPageKindSchema`, capability helpers (`pageAcceptsSections`, `canonicalPageKindFor`, `isNavigablePageKind`), `ProjectPageSchema` |
| `section-shortcut.ts` **new** | reference-only `SectionShortcutSchema`. Schema only; no operations |
| `project.ts` | `ProjectKindSchema`; `RootProjectSchema` + `SubprojectSchema`; `ProjectSchema` their discriminated union; `completedAt` |
| `section.ts` | required `pageId` |
| `document.ts` | `SCHEMA_VERSION` 2 → 3; `projectPages` and `sectionShortcuts` collections |
| `inputs.ts` | `CreateRootProjectInputSchema` + `CreateSubprojectInputSchema`, unioned as `CreateProjectInputSchema`; `UpdateProjectInputSchema.parentProjectId` no longer nullable; `CreateSectionInputSchema` gains optional `pageId` |
| `index.ts` | export the two new modules |
| `document.test.ts`, `project.test.ts`, `section.test.ts`, `inputs.test.ts` | document/project/section literals and `CreateProjectInput` fixtures gain `kind`, `pageId`, and the new collections |

No `ProjectPageQuerySchema`. Nothing in this phase queries pages except the repository by
`projectId`/`kind`; page selection and filtering are 25.2's, and a query shape with no caller
would be creep.

### Repositories — `packages/repositories/src`

| File | Responsibility |
|---|---|
| `interfaces.ts` | `ProjectPageRepository` (find/list/insert/update). No `remove` |
| `json-repositories.ts` | `JsonProjectPageRepository` filtering by `projectId`/`kind`/`enabled` |
| `data-store.ts` | integrity rules, below |
| `repositories.test.ts` | document literal and project/section factories |

Integrity rules added to `validateDocumentIntegrity`:

- a page's project exists and shares its workspace
- kind agrees with owner kind: `home` and the navigable kinds only on roots, `work` only on
  subprojects
- exactly one `home` per root, exactly one `work` per subproject, at most one of each
  navigable kind per root
- `home` and `work` are always `enabled: true`
- every section's `pageId` exists, belongs to the section's project, and names a kind that
  accepts sections

**Shortcut integrity rules are deferred to 25.4.** The collection is empty in every document
this phase writes, so rules over it would be untestable except against a hand-built document —
dead code guarding nothing. The *schema* and the empty collection still land here, which is
what the roadmap asked for and what keeps 25.4 off a second load break.

### Domain — `packages/domain/src`

| File | Responsibility |
|---|---|
| `project-service.ts` | `pages` dependency; project **and** canonical page in one unit of work; reject a root with a parent, a subproject without one, a foreign-workspace or archived parent, and any kind change; stamp/clear `completedAt` from `Clock` |
| `section-service.ts` | `pages` dependency; `addWithin` resolves the canonical page when `pageId` is absent and validates it when present, **after** `assertProjectActive` so an archived project still fails with the archive refusal; `duplicate` keeps `pageId` |

`ProjectService` gains no service edge — it writes the page through the page repository, which
keeps the graph acyclic.

`settleRows`'s reassign target keeps validating `projectId` and `type` only. Page agreement on
reassign is 25.2's, when a project can have two section-bearing pages; today it cannot, so the
check would be vacuous.

### Host — `apps/prototype-host`

| File | Responsibility |
|---|---|
| `persistence/store.ts` | construct `JsonProjectPageRepository` |
| `api/services.ts` | pass it to `ProjectService` and `SectionService` |
| `api/routes.test.ts` | project factory, document literal, and the three `POST /api/projects` bodies gain `kind` |

No route changes. A body with no `kind` is rejected rather than defaulted.

### MCP — `packages/mcp-tools`

**Not optional, and not deferrable to 25.2.** `src/tools/projects.ts:28` is
`CreateProjectInputSchema.omit({ workspaceId: true })`, and `.omit()` does not exist on a
discriminated union — this is a compile error and a runtime `TypeError` the moment the union
lands.

| File | Responsibility |
|---|---|
| `src/tools/projects.ts` | `create_project`'s input becomes a union of the two branches, each `.omit({ workspaceId: true })`; description says how to make each kind |
| `test/harness.ts` | the foreign project fixture gains `kind` and a home page |
| `src/contract.test.ts`, `src/activity.test.ts` | `create_project` inputs gain `kind` |

`registry.test.ts` already asserts `z.toJSONSchema(tool.inputSchema)` does not throw; that must
still pass on the union, which is the check that this rewrite is agent-discoverable. Richer
root/subproject tool *descriptions* remain 25.2's.

### Web — `apps/web`

The plan's earlier claim that Angular was untouched was wrong. Two production call sites break:

| File | Responsibility |
|---|---|
| `core/shell/shell-store.ts:100` | `projects.create({ workspaceId, name })` → add `kind: 'root'` |
| `features/projects/sections/sub-projects/sub-projects-store.ts` | `create({ workspaceId, parentProjectId, name })` → add `kind: 'subproject'` |
| `core/gateway/testing/fake-gateway.ts` | fixtures gain kinds and pages; its `create` branches on `parentProjectId` and its `applyProjectUpdate` is a parallel copy of the update rules — both must learn immutable `kind` and derived `completedAt` deliberately |
| `prototype/design-lab/design-lab-fixtures.ts`, `prototype/design-lab/panels/live-panels.ts` | `labSection` and friends gain `pageId` — these feed Storybook as well as the design lab |
| the spec files that call `ProjectSectionSchema.parse` | `pageId`, listed below |

Spec/story files needing `pageId` or `kind`: `core/gateway/prototype-work-manager-gateway.spec.ts`,
`core/shell/shell-store.spec.ts`, `features/projects/project-page.spec.ts`,
`project-page-store.spec.ts`, `section-removal-dialog.spec.ts`,
`archived-region/archived-region.{spec,stories}.ts`,
`sections/section-frame/project-section-frame.{spec,stories}.ts`,
`features/tasks/task-list-store.spec.ts`, and the seven
`sections/{tasks,reflections,rich-text,progress,timeline,sub-projects,activity}/*.spec.ts`
plus `sections/sub-projects/sub-projects-store.spec.ts`.

These fail at *runtime* rather than type-check, because they parse literals. Expect a red web
suite until they are swept, and sweep them mechanically rather than one test at a time.

### Seeds, converter and fixtures

| File | Responsibility |
|---|---|
| `packages/prototype-data/src/seeds.ts` | `page()` builder; canvases take a page id; every project gets a `kind` and its canonical page |
| `packages/prototype-data/src/seeds.test.ts` | new invariants; the six committed-snapshot cases regenerate |
| `prototype/seeds/*.json` (six) | regenerated snapshots |
| `packages/prototype-data/test/fixtures/nested-projects-v2.json` **new** | the pre-cutover v2 document, committed as a fixture so the converter tests have a corpus that survives the seeds being regenerated |
| `packages/prototype-data/src/upgrade-project-pages.ts` **new** | the converter |
| `packages/prototype-data/src/upgrade-project-pages.test.ts` **new** | its tests |
| `packages/prototype-data/src/upgrade-cli.ts` **new** | CLI, with an injected `UpgradeFileOperations` mirroring `SeedFileOperations` so its failure paths are testable |
| `packages/prototype-data/package.json` | `"upgrade"` script **and** `@cwm/repositories` moved from `devDependencies` to `dependencies` — production `src/` may not import a dev dependency |
| root `package.json` | `"prototype:upgrade"` passthrough |
| `packages/domain/test/test-support.ts` | fixture factories emit kinds and pages |

`apps/e2e/seed.ts` needs **no** change: it POSTs to `/prototype/seed` and constructs nothing.

### Living documentation

`development.md`'s 25.1 row and the roadmap's status header move in this same change.

### The converter

`upgradeProjectPages(document: unknown): { document: PrototypeDocument; changed: boolean }`.

- v3 in → returned unchanged, `changed: false`.
- neither v2 nor v3 → throws naming the version found.
- v2 in → no `parentProjectId` becomes `kind: 'root'` + a `home` page; with one becomes
  `kind: 'subproject'` + a `work` page; every section gains its project's new `pageId`. Ids,
  names, configs, positions, `archivedAt`, `archivedWithSectionId` and timestamps copy through
  untouched. Page ids derive deterministically from the project id, so re-running the same
  input yields the same document.
- The result goes through `validateDocumentIntegrity` **before** anything is written.
- A v2 project with **no sections** still gets its page — `nested-projects`' "Cabinets" is
  exactly this case and is already pinned by `seeds.test.ts`.

The CLI writes `<path>.backup-<timestamp>.json`, then the target through temp-file-and-rename,
the way `seed-cli.ts` does. On any failure it writes nothing and exits non-zero.

One converter for one cutover. No registry, no version chain, no rollback framework.

## Test plan — written first, in this order

### `packages/contracts/src/project.test.ts` (extend)

1. `a root project cannot carry a parent`.
2. `a subproject requires a parent`.
3. `a project without a kind is rejected` — the cutover is not silently defaulted.

### `packages/contracts/src/project-page.test.ts` **new**

4. `home and work accept sections; todos and archive do not`.
5. `the canonical page kind follows the owner kind`.

### `packages/repositories/src/data-store.test.ts` (extend)

6. `a root with two home pages is rejected`.
7. `a subproject with no work page is rejected`.
8. `a navigable page on a subproject is rejected` — a subproject cannot gain tabs at the
   storage layer, not only in the service.
9. `a section on a page of another project is rejected`.
10. `a section on a todos page is rejected`.
11. `a disabled home page is rejected`.

### `packages/domain/src/project-service.test.ts` (extend — note `src/`, not `test/`)

12. `creating a root creates exactly one enabled home page`.
13. `creating a subproject creates exactly one work page and no home`.
14. `a failed page insert leaves no orphan project`. **Mechanism**, since the domain package has
    no failure-injection idiom to copy and `buildHarness` exposes no repository seam: page ids
    derive from the project id and project ids come from `CountingIdGenerator`, so the test
    pre-inserts a page carrying the id the create is about to derive. `JsonCollectionRepository.insert`
    raises `RepositoryConflictError`, and the test then asserts `projects.find(...)` is `null`.
    No new seam, and it exercises the real rollback in `data-store.ts`.
15. `a subproject needs a live parent in the same workspace` — missing, foreign, archived.
16. `a cycle is still rejected`. **The existing test's setup must change**: it currently writes
    a parent onto a root straight through the repository (`project-service.test.ts:205`), which
    the union now rejects at commit as a `DocumentIntegrityError` rather than the expected
    `DomainRuleError`. Both projects become subprojects under a shared root.
17. `update cannot change a project's kind` — including `parentProjectId: null` on a
    subproject, the promotion path that exists today.
18. `completing a project stamps completedAt from the Clock, and reopening clears it`.
19. `creating a project with projects.write alone still works` — the page insert must not start
    requiring a read grant, the same rule `require`'s comment already states.

### `packages/domain/src/section-service.test.ts` (extend)

20. `a section with no page lands on the canonical page` — root → home, subproject → work.
21. `a section cannot be created on another project's page`.
22. `duplicate keeps the original's page`.
23. `an archived project still refuses with the archive error, not a page error` — pins the
    ordering against `assertProjectActive`.
24. `resolveContainer still works for an agent granted tasks.write alone`.

### `packages/prototype-data/src/upgrade-project-pages.test.ts` **new**

25. `converts the committed v2 fixture, preserving ids, section order and archive markers` —
    naming the archived-section case whose rows carry `archivedWithSectionId`.
26. `nested v2 projects become subprojects with a work canvas`.
27. `a v2 project with no sections still gets its page`.
28. `a v3 document is returned unchanged`.
29. `an unsupported schema version fails clearly`.
30. `a conversion that fails validation writes nothing` — asserted on injected file operations:
    no backup, no temp, no target.
31. `the backup is written before the target` — and `a failed backup write aborts before the
    target is touched`.

### `packages/prototype-data/src/seeds.test.ts` (extend)

32. `every seeded project has exactly one canonical page` and `every seeded section names a
    page of its own project`. The six committed-snapshot cases regenerate as part of this.

## Boundaries touched

- **Domain depends on repository abstractions only** (§12). Both services gain a
  `ProjectPageRepository`, not a service. Graph stays acyclic — verified against
  `section-service.ts` (repositories only) and `task-service.ts`/`reflection-service.ts` (both
  compose `SectionService`).
- **No `new Date()` in domain** (§45). `completedAt` comes from the injected `Clock`;
  `time-lint.test.ts` and `scripts/check-no-direct-date.mjs` already guard it.
- **Contracts defined once** (§11). Page kinds, capabilities and the shortcut shape live only
  in `packages/contracts`. The converter reads untyped input and validates only its *output*,
  so it declares no second copy of the v2 shape.
- **Dependency direction.** Moving `@cwm/repositories` into `prototype-data`'s `dependencies`
  is the fix, not the violation: `scripts/check-package-imports.mjs` only scans the domain, so
  nothing would have caught a production import of a dev dependency until `pnpm build`.
- **Duplicated rules, knowingly.** `fake-gateway.ts`'s `applyProjectUpdate` already
  reimplements `ProjectService.update`'s field application. It has to learn immutable `kind`
  and derived `completedAt`. Noted so it is updated deliberately rather than discovered.
- **Deliberately disposable** (§71). One converter, one CLI. No migration framework.

## Explicit non-goals

- page selection, filtering, toggling, any page operation — 25.2
- shortcut *operations* and shortcut integrity rules — 25.4
- archived-ancestor visibility across read models — 25.2
- routing, secondary sidebar, subproject canvases, description/due-date UI — 25.3
- Todos, Archive, Reflections renderers and services — 25.5–25.7
- richer MCP tool descriptions for the two kinds — 25.2 (the schema rewrite is unavoidable here)
- page agreement on section reassign — 25.2
- permanent deletion, bulk restore, root/subproject conversion, subtree moves across roots
- any general migration framework

## Open questions

None blocking. One noted: **`ordered(projectId)` stays project-scoped.** Every v3 project has
exactly one section-bearing page, so project-scoped and page-scoped ordering are the same set.
25.2 makes it page-scoped when a second such page can exist.

## Revisions

- **Review round 1** returned findings in every category, all substantive, all incorporated.
  The three largest were things the plan had asserted without checking: `create_project`'s MCP
  input schema calls `.omit()` on what is about to become a discriminated union, which is a
  hard break this phase cannot defer; two *production* Angular call sites create projects and
  so need a `kind`, contradicting the plan's claim that Angular was untouched; and
  `@cwm/repositories` is a `devDependency` of `prototype-data`, which the converter's
  production import would violate. The review also corrected the domain test paths
  (`src/`, not `test/`), removed `apps/e2e/seed.ts` (it constructs nothing), removed
  `ProjectPageQuerySchema` and the shortcut integrity rules as creep, supplied a
  failure-injection mechanism for test 14 where the package has no idiom to copy, caught that
  the existing cycle test's setup becomes invalid under the union, caught the `parentProjectId:
  null` promotion path the union would turn into a raw `ZodError`, and pointed out that
  acceptance item 3's "pre-cutover v2 file" is unreachable once this same change regenerates
  the seeds — hence the committed v2 fixture. It also found that the decision entry's "can
  never own pages" and its fifth-page-kind tripwire had gone stale against §26; both are
  corrected in the decision entry itself.
